import * as React from 'react';

export const PureSection: React.FC<React.ComponentPropsWithoutRef<'div'>> = () => (
    <button className="pure-button">
        Test
    </button>
);

export default PureSection;
